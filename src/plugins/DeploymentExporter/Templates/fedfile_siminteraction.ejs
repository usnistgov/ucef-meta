<% /* siminteraction(interaction,parameters,children) */ -%>
      (class <%= interaction.name %> <%= interaction.delivery %> <%= interaction.order %>
<% parameters.forEach(function(param){ -%>
<% if(!param.inherited) { -%>
        (parameter <%= param.name %>)
<% } -%>
<% }); -%>
<% children.forEach(function(child){ %><%= child %><% }); -%>
      )
