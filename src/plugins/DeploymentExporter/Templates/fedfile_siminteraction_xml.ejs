<% /*siminteraction(interaction,parameters,children) */ -%>
<interactionClass>
    <name><%= interaction.name %></name>
    <sharing><%=interaction.sharing %></sharing>
    <transportation><%= interaction.delivery %></transportation>
    <% if(!parameters && !children){ -%>
    <interactionClass></interactionClass>
    <%} else{ %>
    <% parameters.forEach(function(param){ %>
    <parameter> 
        <name><%= param.name %></name>
        <dataType><%= param.parameterType %></dataType>
        <semantics></semantics>
    </parameter>
    <% }); -%>
    <% if(children.length!=0) {children.forEach(function(child){ %>
            <%- child %>
    <% });} -%>
    <% } -%>
</interactionClass>